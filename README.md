# IPLCaseStudy-Visualization
I built a data analysis pipeline on IPL datasets using Pandas, then creating visualizations using matplotlib.pyplot. The goal is not just analysis, but to simulate a real-world data workflow: Ingest → Clean → Transform → Analyze → Report → Export You will work with two datasets: deliveries.csv and matches.csv.
